document.addEventListener("DOMContentLoaded", function () {
  renderSkills();
  initModal();
  initContactValidation();
  initThemeToggle();
  projectSearch();
  scrollProgress();
  initGeolocation();
  initGreeting();
  initTyping();
  initSaveContact();
  initBackToTop();
});
